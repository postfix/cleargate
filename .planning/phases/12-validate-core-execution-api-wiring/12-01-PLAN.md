---
wave: 1
depends_on: []
files_modified: []
autonomous: true
---

# Wave 1: Verification of Core Execution and API Wiring

## Objective
Formally test and verify the requirements originally implemented in Phase 07 (EXEC-01, EXEC-03, EXEC-04, EXEC-05, UI-01, UI-02, UI-03) and capture the results in `12-VERIFICATION.md`.

## Tasks

```xml
<task>
  <action>Review `internal/api/execute.go` to confirm that `EXEC-04` (Compile safe argv) and `EXEC-05` (Prevent arbitrary execution) are correctly implemented. Ensure the payload is mapped strictly into an array of arguments, not executed through `sh -c`. Document the verification in `12-VERIFICATION.md`.</action>
  <read_first>
    - internal/api/execute.go
  </read_first>
  <acceptance_criteria>
    - Verification documented for EXEC-04 and EXEC-05.
  </acceptance_criteria>
</task>

<task>
  <action>Review `internal/api/execute.go` to verify `EXEC-03` (Stream status/logs) is implemented properly via Server-Sent Events (SSE) in `HandleEvents`. Document the verification.</action>
  <read_first>
    - internal/api/execute.go
  </read_first>
  <acceptance_criteria>
    - Verification documented for EXEC-03.
  </acceptance_criteria>
</task>

<task>
  <action>Review the React frontend components (`web/src/pages/CatalogPage.tsx`, `web/src/pages/ExecutionPage.tsx`, etc.) to confirm `UI-01` (Dynamic forms), `UI-02` (View catalog), and `UI-03` (Apply presets) are functional. Document the verification.</action>
  <read_first>
    - web/src/pages/ExecutionPage.tsx
  </read_first>
  <acceptance_criteria>
    - Verification documented for UI-01, UI-02, and UI-03.
  </acceptance_criteria>
</task>

<task>
  <action>Confirm that `EXEC-01` (Sandboxed execution) is satisfied end-to-end, connecting the React UI to the Podman backend. Document the overall success in `12-VERIFICATION.md`.</action>
  <read_first>
    - .planning/ROADMAP.md
  </read_first>
  <acceptance_criteria>
    - Verification documented for EXEC-01.
  </acceptance_criteria>
</task>

<task>
  <action>Create `.planning/phases/12-validate-core-execution-api-wiring/12-VERIFICATION.md` with the standard template containing all tests and results.</action>
  <read_first>
    - .planning/ROADMAP.md
  </read_first>
  <acceptance_criteria>
    - `12-VERIFICATION.md` is fully populated and reflects 'passed' status.
  </acceptance_criteria>
</task>
```

## Verification
- `12-VERIFICATION.md` is present and details the pass state of all 7 requirements.
