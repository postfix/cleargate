---
wave: 1
depends_on: []
files_modified: []
autonomous: true
---

# Wave 1: Verification of Rootless Execution & ToolSpec Seeding

## Objective
Formally test and verify the requirements originally implemented in Phase 06 (EXEC-06, EXEC-07, UI-04, TOOL-05) and capture the results in `11-VERIFICATION.md`.

## Tasks

```xml
<task>
  <action>Review `internal/runtime/podman.go` to ensure `EXEC-06` (Real Podman Integration) and `EXEC-07` (Security Profile: rootless, cap-drop, read-only) are correctly implemented. If tests are missing, implement them or manually verify behavior by executing a test job. Document the verification in `11-VERIFICATION.md`.</action>
  <read_first>
    - internal/runtime/podman.go
  </read_first>
  <acceptance_criteria>
    - Verification documented for EXEC-06 and EXEC-07.
  </acceptance_criteria>
</task>

<task>
  <action>Review `internal/repository/toolspec_repo.go` to verify `TOOL-05` (Database Seeder) successfully parses `.yaml` files from the `tools/` directory and inserts them into DuckDB. Document the verification.</action>
  <read_first>
    - internal/repository/toolspec_repo.go
  </read_first>
  <acceptance_criteria>
    - Verification documented for TOOL-05.
  </acceptance_criteria>
</task>

<task>
  <action>Review the React frontend (e.g., `web/src/pages/CatalogPage.tsx` and `web/src/pages/ExecutionPage.tsx`) to ensure `UI-04` (Remove UI Mocks) is satisfied and the application fetches the real catalog from the backend API. Document the verification.</action>
  <read_first>
    - web/src/pages/CatalogPage.tsx
  </read_first>
  <acceptance_criteria>
    - Verification documented for UI-04.
  </acceptance_criteria>
</task>

<task>
  <action>Create `.planning/phases/11-validate-rootless-execution-toolspec-seeding/11-VERIFICATION.md` with the standard template containing all tests and results.</action>
  <read_first>
    - .planning/ROADMAP.md
  </read_first>
  <acceptance_criteria>
    - `11-VERIFICATION.md` is fully populated and reflects 'passed' status.
  </acceptance_criteria>
</task>
```

## Verification
- `11-VERIFICATION.md` is present and details the pass state of all 4 requirements.
