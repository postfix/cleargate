---
wave: 1
depends_on: []
files_modified: []
autonomous: true
---

# Quick Plan: Add a job cancellation API endpoint, implement runtime.Stop() for Podman, and add a Stop button to the ExecutionPage UI

## Tasks

```xml
<task>
  <action>Add `Stop(ctx context.Context, id ContainerID) error` to `ContainerRuntime` in `internal/runtime/runtime.go`, and implement it in `internal/runtime/podman.go` by invoking `containers.Kill(podman.Ctx, string(id), nil)`.</action>
  <read_first>
    - internal/runtime/runtime.go
    - internal/runtime/podman.go
  </read_first>
  <verify>Check that `DummyRuntime` and `PodmanRuntime` both implement the new `Stop` method.</verify>
  <done>false</done>
</task>

<task>
  <action>Create a new API handler `HandleCancelJob` in `internal/api/execute.go`. It should map `job_id` to a `ContainerID` (e.g. "cleargate-job-{jobID}") and call `runtime.Stop()`. Register the route `DELETE /api/jobs/{id}` in `cmd/cleargate/main.go`.</action>
  <read_first>
    - internal/api/execute.go
    - cmd/cleargate/main.go
  </read_first>
  <verify>Check that the DELETE endpoint parses the job ID and correctly targets the running container.</verify>
  <done>false</done>
</task>

<task>
  <action>Update `web/src/pages/ExecutionPage.tsx` to include a "Stop" button in the `status-bar` when `jobStatus === 'running'`. It should send a `DELETE /api/jobs/${activeJobId}` request and set the status to `stopped`.</action>
  <read_first>
    - web/src/pages/ExecutionPage.tsx
  </read_first>
  <verify>Check that the UI handles the cancel API gracefully and updates the jobStatus indicator.</verify>
  <done>false</done>
</task>
```
